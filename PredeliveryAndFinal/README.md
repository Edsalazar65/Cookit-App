# How to run the web app
1. Inside ```backend/``` run ```npm install ```
2. Create ```backend/.env``` using ```.env.example```
3. Create ```web_frontend/assets/js/firebase-init.js``` using ```firebase-init.example.js```
